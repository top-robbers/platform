meta {
  name: Server Status
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/api/v1/server/status
  body: none
  auth: bearer
}

headers {
  Accept: application/json
}

auth:bearer {
  token: {{apiToken}}
}